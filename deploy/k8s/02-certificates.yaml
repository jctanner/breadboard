---
# Certificate for GitHub Emulator
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: github-emulator-tls
  namespace: ai-pipeline
spec:
  secretName: github-emulator-tls
  duration: 8760h # 1 year
  renewBefore: 720h # 30 days

  commonName: github-emulator.ai-pipeline.svc.cluster.local

  dnsNames:
    - github-emulator
    - github-emulator.ai-pipeline
    - github-emulator.ai-pipeline.svc
    - github-emulator.ai-pipeline.svc.cluster.local
    - github.local

  ipAddresses:
    - 192.168.56.10

  privateKey:
    algorithm: RSA
    size: 2048

  issuerRef:
    name: internal-ca-issuer
    kind: Issuer
---
# Certificate for the Fullsend operations dashboard
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fullsend-dashboard-tls
  namespace: ai-pipeline
spec:
  secretName: fullsend-dashboard-tls
  duration: 8760h
  renewBefore: 720h
  commonName: fullsend-dashboard.ai-pipeline.svc.cluster.local
  dnsNames:
    - fullsend-dashboard
    - fullsend-dashboard.ai-pipeline
    - fullsend-dashboard.ai-pipeline.svc
    - fullsend-dashboard.ai-pipeline.svc.cluster.local
    - fullsend.local
  ipAddresses:
    - 192.168.56.10
  privateKey:
    algorithm: RSA
    size: 2048
  issuerRef:
    name: internal-ca-issuer
    kind: Issuer
---
# Certificate for Pipeline Dashboard
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pipeline-dashboard-tls
  namespace: ai-pipeline
spec:
  secretName: pipeline-dashboard-tls
  duration: 8760h # 1 year
  renewBefore: 720h # 30 days

  commonName: pipeline-dashboard.ai-pipeline.svc.cluster.local

  dnsNames:
    - pipeline-dashboard
    - pipeline-dashboard.ai-pipeline
    - pipeline-dashboard.ai-pipeline.svc
    - pipeline-dashboard.ai-pipeline.svc.cluster.local
    - dashboard.local

  ipAddresses:
    - 192.168.56.10

  privateKey:
    algorithm: RSA
    size: 2048

  issuerRef:
    name: internal-ca-issuer
    kind: Issuer
---
# Certificate for GitLab Emulator
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab-emulator-tls
  namespace: ai-pipeline
spec:
  secretName: gitlab-emulator-tls
  duration: 8760h # 1 year
  renewBefore: 720h # 30 days

  commonName: gitlab-emulator.ai-pipeline.svc.cluster.local

  dnsNames:
    - gitlab-emulator
    - gitlab-emulator.ai-pipeline
    - gitlab-emulator.ai-pipeline.svc
    - gitlab-emulator.ai-pipeline.svc.cluster.local
    - gitlab.local

  ipAddresses:
    - 192.168.56.10

  privateKey:
    algorithm: RSA
    size: 2048

  issuerRef:
    name: internal-ca-issuer
    kind: Issuer
---
# Certificate for Jira Emulator
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jira-emulator-tls
  namespace: ai-pipeline
spec:
  secretName: jira-emulator-tls
  duration: 8760h # 1 year
  renewBefore: 720h # 30 days

  commonName: jira-emulator.ai-pipeline.svc.cluster.local

  dnsNames:
    - jira-emulator
    - jira-emulator.ai-pipeline
    - jira-emulator.ai-pipeline.svc
    - jira-emulator.ai-pipeline.svc.cluster.local
    - jira.local

  ipAddresses:
    - 192.168.56.10

  privateKey:
    algorithm: RSA
    size: 2048

  issuerRef:
    name: internal-ca-issuer
    kind: Issuer
---
# Certificate for the development Fullsend token mint.
# Fullsend refuses a non-HTTPS mint URL at install time, and the mint now
# receives OIDC assertions that should not cross the cluster in the clear.
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fullsend-mint-dev-tls
  namespace: ai-pipeline
spec:
  secretName: fullsend-mint-dev-tls
  duration: 8760h # 1 year
  renewBefore: 720h # 30 days

  commonName: fullsend-mint-dev.ai-pipeline.svc.cluster.local

  dnsNames:
    - fullsend-mint-dev
    - fullsend-mint-dev.ai-pipeline
    - fullsend-mint-dev.ai-pipeline.svc
    - fullsend-mint-dev.ai-pipeline.svc.cluster.local
    - mint.local

  privateKey:
    algorithm: RSA
    size: 2048

  issuerRef:
    name: internal-ca-issuer
    kind: Issuer
