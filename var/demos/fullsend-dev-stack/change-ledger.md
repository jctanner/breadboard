# Change ledger

| ID | Milestone | Change | Reason | Status |
|---|---|---|---|---|
| M0-001 | M0 | Defined the first compatibility scenario as a manually dispatched `triage` workflow against `fullsend-dev/triage-target`, expecting a marked issue comment. | Establishes a deterministic, low-side-effect result before testing code/PR mutation. | Complete |
| M0-002 | M0 | Selected a development-only mint adapter returning emulator PATs, with vendored/local harnesses for the first run. | Avoids requiring GitHub App installation and public raw-content infrastructure in the first stack. | Complete |
| M1-001 | M1 | Added the k3s GitHub Actions runner deployment and deterministic local runner image build/import path. | Fullsend needs a GitHub Actions execution surface; the existing stack only deploys GitLab Runner. | Complete; run 44/job 88 succeeded |
| M1-002 | M1 | Added CoreDNS rewrite configuration for breadboard `.local` service names, explicitly restarted CoreDNS during deployment, and mounted the internal CA into the runner. | Runner and future OpenShell jobs need to reach `github.local` while preserving the original ingress Host/SNI routing and TLS verification. | Complete; pod DNS/API checks passed |
| M1-003 | M1 | Fixed the GitHub emulator Actions long-poll path to call `AsyncSession.expire_all()` synchronously and use a captured repository ID after expiration. | The runner received HTTP 500 every time it returned to an idle poll after completing a job. | Complete; idle poll returned 204 |
| M2-001 | M2 | Added the GitHub Actions `workflow_dispatch` endpoint plus rendering for dispatch inputs, repository variables, and development-only secret values in job environments. | Fullsend’s stage workflows are dispatch-driven and pass configuration through Actions contexts. | Complete; dispatch created run 47 |
| M2-002 | M2 | Added emulator Git checkout support to the lightweight runner and per-step `GITHUB_EVENT_PATH`, `GITHUB_OUTPUT`, and `GITHUB_ENV` files. | The reduced Fullsend-shaped workflow needs repository content and standard Actions command-file behavior. | Complete; checkout and command-file checks passed |
| M2-003 | M2 | Added a repeatable reduced Fullsend-shaped seed/dispatch workflow. | Provides a deterministic M2 exit without invoking Vertex, an LLM, or OpenShell. | Complete; run 47/job 91 succeeded |
| M3-001 | M3 | Added the breadboard-local `fullsend-mint-dev` service, role PAT bootstrap, Kubernetes Secret, and deployment wiring. | The emulator has PATs but no production GitHub App/OIDC mint; Fullsend needs the `/v1/token` contract in the development stack. | Complete; service rollout and role-token bootstrap succeeded. Repository restriction is declarative because emulator PAT scope enforcement is not implemented. |
| M3-002 | M3 | Added local OIDC response handling, Bash execution for composite actions, runtime step-output expressions, and log masking to the lightweight Actions runner. | Fullsend's `mint-token` action uses the Actions OIDC request environment, Bash syntax, and `steps.*.outputs.*`; raw PATs must not appear in emulator job logs. | Complete; run 56/job 100 succeeded |
| M3-003 | M3 | Added the repeatable M3 seed/dispatch workflow and Vertex credential-file smoke check. | Separates credential/backend initialization from the later Vertex API, Fullsend agent, and OpenShell milestones. | Complete; run 56/job 100 authenticated and loaded Vertex credentials |
| M4-001 | M4 | Added the Fullsend/OpenShell host-runner and sandbox image build/import script, using a clean `git archive` plus a checked-in patch. | A wiped system must rebuild from the pinned checkout without relying on ignored local edits; `-buildvcs=false` is required because the archive has no `.git` metadata. | Complete; images imported to k3s |
| M4-002 | M4 | Added `tar`/`gzip` to the sandbox wrapper and `openssh-client` to the host runner wrapper. | OpenShell file transfer uses archive tools in the sandbox and SSH transport from the host-side CLI; both dependencies were identified by concrete bootstrap failures. | Complete; sandbox bootstrap and file transfer passed |
| M4-003 | M4 | Added the Fullsend compatibility patch for OpenShell's 19-character sandbox-name limit and skipped nested Fullsend-binary upload for the dummy runtime. | Fullsend's default PID/Unix-seconds name was rejected before sandbox creation; the dummy runtime does not need a second Fullsend binary. | Complete; patch is under `patches/fullsend/` |
| M4-004 | M4 | Added local harness materialization through an init container and made dummy `assert_env` source the generated sandbox `.env`. | ConfigMap mounts use symlinks that Fullsend rejects during path validation, and raw dummy commands otherwise could not observe `env.sandbox` values. | Complete; `GH_TOKEN` assertion passed |
| M4-005 | M4 | Added the deterministic M4 Job, explicit local network policy, artifact-holder sidecar, stale-pod cleanup, and retained evidence copy. | Makes the OpenShell path repeatable and preserves output/logs even though the main container exits before `kubectl cp` runs. | Complete; sandbox `agent-triage-3b8d6` exited 0 and artifacts were retained |
| M4-006 | M4→M5 | Confirmed ordinary k3s pods resolve and reach github.local; M5 then verified the same API route from inside an OpenShell sandbox. | Separates cluster DNS/proxy health from OpenShell network-policy compatibility. | Complete; sandbox agent-triage-4ce81 passed the API probe |
| M5-001 | M5 | Fixed Fullsend dummy url_get command construction from curl -sf -- URL -o /dev/null to curl -sf -o /dev/null -- URL. | curl treated -o as a second URL after --, causing OpenShell to deny a bogus http://-o/ request even though the real emulator request was allowed. | Complete; retained behavior result passed |
| M5-002 | M5 | Added explicit OpenShell policies for github.local, Google OAuth, and the configured regional Vertex endpoint; added sandbox host-file transfer for the existing GCP credential JSON and Vertex environment variables. | The sandbox has an independent environment/filesystem boundary and must reach internal and regional endpoints through the policy proxy without exposing credentials in source. | Complete; OAuth and Vertex requests were allowed; metadata-server requests remained denied |
| M5-003 | M5 | Added the repeatable 20-run-fullsend-m5-vertex.sh smoke and mapped the Fullsend harness name to Claude's valid built-in claude agent identifier. | Fullsend passes the selected name to Claude Code's --agent flag; the local role name triage is not a Claude built-in agent. | Complete; sandbox agent-claude-6d8ed, Fullsend status 0 |
| M6-001 | M6 | Added the repeatable 21-run-fullsend-m6-result.sh smoke with a generated host-side post-triage script and emulator API verification. | M6 needs a visible emulator-side result while the OpenShell sandbox remains read-only; Fullsend's post-script boundary provides the intended host-side mutation point. | Complete; marked comment verified on fullsend-dev/triage-target#1 |
| M6-002 | M6 | Carried the M5 Vertex setup into M6, removed `--no-post-script` safely from the generated shell command, and made the generated post-script POSIX `/bin/sh` compatible. | The first generated M6 manifest retained the skip flag and then exposed a shell-array failure; both prevented the result comment despite a successful agent run. | Complete; clean rerun passed |
| M6-003 | M6 | Verified the marked comment and retained sanitized emulator comment metadata with the Claude/OpenShell artifacts. | Makes the emulator-side result independently inspectable after the Job and sandbox are deleted. | Complete; sandbox agent-claude-8a772, trace a6f3146f-db26-4f0c-a780-fbaf01e70a37 |
| M7-001 | M7 | Switched the emulator Actions runner Deployment to `fullsend-runner-dev:k3s` and supplied the OpenShell gateway endpoint/name. | The lightweight runner already executes shell steps; the Fullsend/OpenShell image lets that same runner invoke the proven agent path. | Complete; runner `fullsend-dev-runner` rolled out and accepted run 62 |
| M7-002 | M7 | Added `m7_seed.py`, which pushes a dispatchable workflow and local `.fullsend` harness/policy into the emulator before dispatching it. | The combined path needs repository-local harness files and a repeatable event-to-result trigger rather than a manually applied Kubernetes Job. | Complete; workflow 23 dispatched successfully |
| M7-003 | M7 | Fixed the shared emulator policy asset so `binaries` is a network-policy sibling of `endpoints`, and blanked inherited `FULLSEND_MINT_URL` for the Actions job. | OpenShell rejected the initially seeded policy schema; the runner's global mint URL otherwise forced an unintended host token-mint path. | Complete; corrected run 62 reached sandbox creation |
| M7-004 | M7 | Verified the complete Actions-to-emulator path and recorded the resulting run/job IDs and comment-count transition. | Confirms the event, runner, Fullsend, OpenShell, and emulator result are connected in one repeatable scenario. | Complete; run 62/job 106, comment count 1 → 2 |
| M8-001 | M8 | Added `m8_protocol_check.sh` to run the emulator's focused Actions runner contract tests. | Establishes a repeatable baseline for registration, polling, log upload, completion, and the compatibility broker before introducing the upstream runner binary. | Complete; 5 tests passed. This is emulator-contract evidence, not yet upstream-binary validation. |
| M8-002 | M8 | Added an isolated real-runner image/smoke path using the upstream `actions/runner` binary and a dedicated `fullsend-real` label. Fixed runner-facing public URL generation, internal-CA trust, transient registration retries, label-subset matching, and the artificial queued-job window. | Separates upstream runner protocol validation from the proven lightweight M7 runner while asserting emulator job logs and completion. | Complete; runner 2.317.0, workflow run 74/job 118, success with marker `m8-upstream-runner-ok`. |
| M8-003 | M8 | Added resettable GitHub App, installation, selected-repository installation-token, and ephemeral RS256 OIDC/JWKS endpoints to the emulator, plus idempotent `m8_seed.py` bootstrap. | The production Fullsend mint consumes an OIDC JWT and GitHub App installation API; the prior M3 PAT adapter could not exercise that contract. | Complete; emulator App/OIDC tests passed and live App fixture seeded. |
| M8-004 | M8 | Added generic `OIDC_ISSUER_URL`, `GITHUB_API_URL`, and opt-in `NO_SSL_VERIFY=1` handling to the local standalone Fullsend mint path, with `m8_standalone_mint_smoke.sh`. | The real mint was hard-coded to GitHub.com and could not reach the breadboard emulator's self-signed `.local` endpoints. Defaults remain production-safe. | Complete; real standalone mint returned a selected-repository `ghs_` token. |
| M8-005 | M8 | Added `m8_mirror.py` for selected local Fullsend actions and a role/event fixture with triage, review, and coder matrix jobs. | Keeps the emulator fixture repeatable and network-independent while broadening beyond the first triage-only workflow. | Complete; run 77 completed jobs 121–123 successfully. |
| M8-006 | M8 | Preserved matrix expressions and job permissions, added JSON-backed Actions artifacts, concurrency-group cancellation, and explicit cancellation regression tests. | These behaviors affect real workflow scheduling and evidence retention and were previously absent or unverified in the emulator path. | Complete; 14 focused Actions/App/OIDC tests passed. |
| M9-000 | M9 | Defined the next target as the pinned upstream Fullsend triage harness and reusable workflow, replacing the synthetic M7 prompt/post-script path. | `triage-target` previously validated execution plumbing and a reduced local harness, but did not prove that Fullsend's actual triage agent could run and produce its own emulator-side result. | Complete; pinned `fullsend-ai/agents` triage path exercised by run 113/job 161 |
| M9-001 | M9 | Added the pinned Fullsend triage harness/resources to the seeded target repository, installed pinned `gh` and `jsonschema` in the runner/sandbox images, mapped the upstream GitHub configuration to `github.local`, refreshed the OpenShell Vertex profile, and added a `NO_SSL_VERIFY=1`-gated HTTP mint-client patch. | The real agent requires its upstream harness, GitHub CLI, schema validator, Vertex/OpenShell access, and token mint; the emulator mint is intentionally plain HTTP inside the isolated cluster. The local profile needed explicit Vertex endpoints and Claude CLI binary paths. HTTPS remains mandatory unless the development opt-in is present. | Complete; run 113/job 161 ran Claude through OpenShell, passed output validation, and completed the upstream post-script |
| M9-002 | M9 | Added `GH_ENTERPRISE_TOKEN` alongside the upstream-required `GH_TOKEN` in the local runner/sandbox harness environment. | GitHub CLI treats `github.local` as a GitHub Enterprise Server host and does not use `GH_TOKEN` for that host; it requires the enterprise-token variable. The same minted token is used, so upstream GitHub.com behavior remains unchanged. | Complete; fixed the post-triage label/comment HTTP 401 |
| M9-003 | M9 | Changed the M9 verification to require a new issue comment rather than requiring the sticky `fullsend:triage-agent` marker for every result. | The real upstream agent can validly choose `insufficient`; its post-script posts a normal clarifying-question comment for that action and reserves the sticky marker for `sufficient`. The seed script compares comment IDs before and after the run. | Complete; run 113 added one new agent comment; issue #1 result was `insufficient` |
| M10-001 | M10 | Added `m10_seed.py` to seed the pinned Fullsend review harness, review skills/sub-agents, local mint action, first-review defaults, and a dispatchable PR workflow. | Makes the real `review` role repeatable from the checked-out Fullsend sources and avoids relying on public raw-content URLs at runtime. | Complete; PR #4 and workflow 27 seeded in `fullsend-dev/triage-target`. |
| M10-002 | M10 | Added local GitHub Enterprise environment mapping, explicit Vertex/OpenShell profile endpoints and Claude binary paths, and reused the role-scoped review token. | The review runner/sandbox must reach `github.local`, the self-signed emulator CA, Vertex, and the local mint while preserving production-safe upstream defaults. | Complete; the review agent ran through OpenShell and passed structured-output validation. |
| M10-003 | M10 | Patched the seeded `post-review.sh` file lookup from `gh pr view --json files` to `gh api repos/{repo}/pulls/{pr}/files --paginate --jq '.[].filename'`. | The emulator implements changed files through REST while its GraphQL `PullRequest.files` field is a stub; REST is also supported by GitHub.com and GitHub Enterprise. | Complete; run 122/job 170 posted an `APPROVE` review and marked Fullsend comment. |
| M11-001 | M11 | Implemented the generic Actions activity dispatcher coverage for issues (`opened`, `edited`, `labeled`), issue comments (`created`), pull-request targets (`opened`, `synchronize`, `ready_for_review`, `closed`, `labeled`, `unlabeled`), and pull-request reviews (`submitted`), including push-to-PR synchronization. | Fullsend triage and review are event-driven in production; manual dispatch alone cannot reproduce their behavior. | Complete; 15 focused emulator event-trigger tests passed. |
| M11-002 | M11 | Exposed the complete event payload as `github.event`, added `||` fallback expression handling for automatic/manual fixtures, normalized created review states to GitHub values, and prevented label-only issue updates from emitting a spurious `edited` event. | Fullsend workflows need event-derived issue/PR numbers and GitHub-compatible review/label activity semantics. | Complete; payload, actor, label, comment, review, and synchronization assertions pass. |
| M11-003 | M11 | Updated the M9 triage and M10 review seed workflows to subscribe to automatic event triggers while retaining `workflow_dispatch` fallback inputs. | The local Fullsend fixtures should be runnable from issue/PR activity and still support deterministic operator replay. | Complete in seed definitions; live automatic-agent replay remains the next integration check. |
| M11-004 | M11 | Added `created`, `deleted`, `forced`, and explicit after-SHA handling to generic push payloads, including branch-state detection during Git receive-pack processing and suppression of PR synchronization after head-branch deletion. | Branch creation/deletion and commits are represented by GitHub's `push` event; they must not be reduced to a default-branch update or an invalid PR synchronization. | Complete; push/PR synchronization regression coverage passes. |
| M11-005 | M11 | Rebuilt/redeployed the GitHub emulator, seeded the event-enabled M9 workflow, created issue #8 without workflow dispatch, and verified the real triage job ran and posted its own result comment. | Confirms the generic event dispatcher works in the deployed stack, not only in unit tests. | Complete; automatic run 133 (`issues`) → job 189 success; comment #8 created by the agent. |
| M11-006 | M11 | Added `GH_ENTERPRISE_TOKEN` to the host-side `.fullsend/env/github/triage.env` generated by `m9_seed.py`, then reseeded the target repository. | The OpenShell agent harness already had the enterprise token, but `post-triage.sh` runs on the host side and `gh` rejected `GH_TOKEN` alone for `github.local` with `401 Bad credentials`. | Complete; corrected seed commit `49e3f87`; issue #10 → automatic run 144 success and comment #9. |
| M11-007 | M11 | Added the same minted token as `GH_ENTERPRISE_TOKEN` to the Actions step environment that launches `fullsend run`. | The previous M11-006 env-file fix covered the sandbox but not the host process that executes `post-triage.sh`; retriggered runs could still fail with 401. | Complete; corrected seed commit `d0410d7`; issue #11 → automatic run 159 success and comment #10. |
| M11-008 | M11 | Added the checked-in Fullsend compatibility patch `0003-honor-github-api-url-for-sticky-comments.patch`, making both legacy and tracker-based sticky-comment clients honor `GITHUB_API_URL`. Rebuilt/imported `fullsend-runner-dev:k3s`. | Job 339's sufficient-triage branch reached the agent successfully but its sticky-comment client used the hard-coded `api.github.com` endpoint, producing `401 Bad credentials` against the local emulator. GitHub.com remains the default when `GITHUB_API_URL` is absent. | Complete; issue #12 → run 216/job 378 success, post-script completed, emulator comment #11 created. |
| M11-009 | M11 | Updated emulator issue-label mutations to accept `gh api`'s `labels[]` form payload and create missing repository labels before attaching them. | Job 587 reported successful label application, but issue #9 remained unlabeled; the duplicate `ready-to-code` create response was expected, while the add-label path silently ignored the CLI payload and unknown `documentation` label. | Complete in emulator tests; deployed validation pending. |
| M12-001 | M12 | Replaced the accumulated target fixtures with a production-shaped workflow-call shim, a separate `.fullsend` dispatcher repository, and a triage-only stage. Added emulator resolution for imported local repositories referenced by remote reusable-workflow calls. | The prior M8/M9/M10 workflows invoked agents directly and allowed fixture-only event feedback loops; production Fullsend filters events in a repo-local shim and routes stages centrally. | Complete; target commit `482edc2`, config commit `94e767d`; issue #21 → target run 553/job 1201 → central run 554/job 1202, success. |
| M12-002 | M12 | Added a second reproducible GitHub Actions runner deployment registered to `fullsend-dev/.fullsend`; retained the existing runner for `fullsend-dev/triage-target`. | The emulator's current runner registration and polling APIs are repository-scoped, so a single target runner cannot claim the central configuration repository's stage jobs. | Complete; both runners online; central triage job 1202 was claimed and completed successfully. |
| M12-003 | M12 | Replaced the direct triage implementation with the production-shaped `triage.yml` → `triage-agent.yml` reusable-workflow call and added recursive emulator materialization with input/secret forwarding, nested dependency remapping, and depth/cycle protection. | Production delegates from the central stage workflow to a reusable agent workflow; one-level flattening was insufficient for 1:1 workflow topology. | Complete; focused emulator contract/tests passed and issue #22 produced config run 602/job 1250, which completed successfully and applied `needs-info`. |
| M12-004 | M12 | Added job-level `if` evaluation for boolean expressions, context property access, and common string predicates; propagated caller conditions through reusable-workflow materialization and marked rejected jobs as skipped. | The Fullsend shim only permits `ready-*` label events. Without this behavior, bot labels such as `duplicate` and `not-planned` retriggered triage on closed issues. | Complete; 24 focused tests passed; deployed `not-planned` validation created target run 637/job 1285 as skipped and created no central run. |
| M12-005 | M12 | Extended the central `.fullsend` dispatcher to route `ready-for-triage`, `ready-to-code`, and `ready-for-review` labels, `/fs-*` commands, and relevant pull-request events to separate thin caller/reusable agent workflows. Added local code and review agent implementations using the role-scoped mint token and `fullsend run code|review`. | The triage-only dispatcher sent `ready-to-code` and `ready-for-review` events back into triage, causing repeated processing and comments instead of advancing the Fullsend stage. | Complete in the repeatable seed; config commit `86745ff`; live code/review execution remains a focused validation step. |
| M12-006 | M12 | Added local reusable-workflow resolution without an `@ref` and applied reusable-workflow materialization to the emulator's `workflow_dispatch` API path. | Central Fullsend stage calls are dispatched with `workflow_dispatch`; without this path using the same materializer as event-triggered runs, nested code/review jobs became empty `ubuntu-latest` placeholders and could not be claimed by the Fullsend runner. | Complete; emulator suite `25 passed`; direct code dispatch run `783` materialized `Code agent` on `self-hosted/linux/fullsend` and was claimed by `fullsend-dev-config-runner`. The agent then correctly stopped because code assets are not yet seeded. |
| M12-007 | M12 | Added runner-side step-level `if` evaluation with previous-step output lookup, preserved step conditions in the emulator job payload, and synchronized the current runner source into the `fullsend-runner-dev` image during its build. | The central dispatcher intentionally skips stage dispatch when no stage matches. The runner previously executed that guarded step anyway, causing no-op issue-comment runs to call an empty workflow URL and fail with 404; rebuilding only the emulator image did not update the separately based Fullsend runner image. | Complete; 28 focused tests passed; rebuilt/redeployed the emulator and Fullsend runner images; smoke run `792`/job `1440` completed successfully with the dispatch step `skipped`. |
| M12-008 | M12 | Added the missing code-agent sandbox dependencies (`gitleaks`, `scan-secrets`, `jq`, and system-level `jsonschema`) and completed the GitHub emulator GraphQL compatibility needed by the real code agent (`PullRequestState`, `baseRefName`, `orderBy`, and `maintainerCanModify`). | The upstream code agent requires mandatory secret scanning and schema validation, and its real `gh pr list`/`gh pr create` queries exercised GraphQL fields that the emulator did not yet implement. The additions preserve upstream defaults and only provide local emulator compatibility. | Complete; emulator GraphQL tests passed; the sandbox image checks passed; Fullsend run `816` completed successfully, creating open PR #25 from `agent/23-add-readme` in `fullsend-dev/triage-target`. Nonfatal warnings remain for PR label/assignment and review-request fields. |
| M12-009 | M12 | Added the local review role and pinned review assets to the centralized `.fullsend` seed, supplied required review defaults, and preserved canonical GitHub PR URLs while routing API calls to `github.local`. | The production-shaped review workflow initially fell back to `fullsend-ai/agents@v0` because the combined config omitted `review`; once local assets were loaded, upstream pre-review validation also required canonical GitHub URL and review-context defaults. | Complete; config commit `b90203f`; run `826`/job `1474` passed schema validation, OpenShell execution, and post-review processing; PR #25 received an `APPROVED` review and agent comment #89. The emulator still reports no `ready-for-merge` label because that label mutation is a nonfatal post-script gap. |
| M12-010 | M12 | Added the GraphQL compatibility required by `gh pr edit --add-label`: paginated `reviewRequests`/project connections, `User`/`Bot`/`Team` reviewer union types, and the `addLabelsToLabelable` mutation backed by repository issue labels. | The upstream Fullsend review post-script uses the GitHub CLI's GraphQL lookup and label mutation; the emulator previously swallowed those failures as nonfatal warnings, so an approved review did not advance to `ready-for-merge`. | Complete; 33 focused emulator tests passed, the rebuilt emulator was deployed, and run `838`/job `1486` completed successfully with no GraphQL post-script error. PR #25 now has `ready-for-merge`. |
| M12-011 | M12 | Added an environment-only model override in the Fullsend seed so every `.fullsend/agents` and review sub-agent definition is emitted with `model: claude-sonnet-5`, without modifying the upstream `fullsend-agents` checkout. | This breadboard environment must use Sonnet 5 consistently across triage, code, review, and review sub-agents; upstream defaults still specify Opus or other models. | Complete; config commit `f310663`; stale queued/in-progress pre-override runs were canceled, and fresh Code run `880` was created from the Sonnet-configured commit. |
| M12-012 | M12 | Made GitHub App installation-token requests act as a dedicated `<app-slug>[bot]` user, while retaining the installation account as the owner. Added a bot-review guard to the target shim. | The emulator attributed Fullsend's installation-token reviews to `admin`, and every resulting `pull_request_review` event re-entered the shim, creating a review feedback loop. | Complete; emulator App tests 9 passed, live minted token resolved to `fullsend-triage[bot]`, and controlled review 41 created no new central `.fullsend` run. |
| M12-013 | M12 | Explicitly propagated each minted GitHub App token into `GITHUB_TOKEN` for triage/code/review and `REVIEW_TOKEN` for review, and added the same mapping to the host-side review environment. | The workflow exposed the token as `GH_TOKEN`, but Fullsend's review post-script consumes `REVIEW_TOKEN` and other tools can fall back to the runner's default `GITHUB_TOKEN` (`admin`), so agent reviews could still retrigger the shim indefinitely. | Complete in the seeded workflow; the remaining actor verification is covered by M12-014. |
| M12-014 | M12 | Changed the repeatable development mint bootstrap to create each role token for the seeded `fullsend-triage[bot]` user, ensuring the App/bot exists before PAT creation. | The development mint was returning `ghp_` PATs owned by `admin`; workflow token propagation alone could not change the authenticated actor, so reviews continued to emit `admin` review events. | Complete; the live `fix` role token resolved to `fullsend-triage[bot]`, and controlled review events were bot-authored. |
| M12-015 | M12 | Added the pinned Fix stage to `m11_seed.py`: target shim routing for bot-authored `CHANGES_REQUESTED` reviews and `/fs-fix`, centralized `fix.yml`/`fix-agent.yml`, the Fix harness/assets, and the existing Sonnet 5 override. | Fullsend's production topology needs review-driven Fix execution in the central `.fullsend` repository. The upstream `checkouts/fullsend-agents` checkout remains read-only; the seed materializes a local, reproducible copy. | Complete; contract tests, Python compilation, generated-asset checks, and live reseeding passed. Config commit `f63ec110`. |
| M12-016 | M12 | Added two local-emulator compatibility adaptations to the generated Fix workflow: fall back from GitHub's synthetic `pull/<number>/head` ref to the same-repository head branch, and export `HUMAN_INSTRUCTION`, `GH_HOST`, and `GITHUB_API_URL` into the sandbox. | The emulator does not currently expose synthetic pull refs, while the pinned Fix harness requires `HUMAN_INSTRUCTION` and the sandbox-side `gh` preflight must target `github.local` instead of `api.github.com`. Production behavior remains the first path/default. | Complete; initial runs 950/952/954 exposed the ref, variable, and sandbox-host failures respectively; each was fixed before the final replay. |
| M12-017 | M12 | Validated the real `CHANGES_REQUESTED` → Fix path with a bot-authored review on PR #27. | Confirms event routing, nested workflow materialization, role-scoped minting, local OpenShell connectivity, Sonnet 5 selection, Fix schema validation, and post-script handling as one integrated flow. | Complete; review #55 → target run `955` → central Fix run `956`/job `1604` succeeded. The test review contained no actionable finding, so the agent correctly made no code commit. |
| M12-018 | M12 | Added emulator GraphQL auto-merge mutations, persisted auto-merge requests, event-driven processing on the `ready-for-merge` gate, and local Code-agent opt-in with squash merges. | Fullsend's Code agent can request `gh pr merge --auto`; the development stack needs a durable queue and a deterministic readiness signal while the emulator lacks GitHub's full branch-protection/check-run calculation. | Implemented and covered by 31 focused emulator tests plus 4 seed contract tests. Emulator deployment and live Fullsend merge smoke remain pending. |
| M12-019 | M12 | Fixed Git Smart HTTP write authorization to honor repository collaborators and made the Fullsend bootstrap grant `fullsend-triage[bot]` push access to `triage-target`. | The Code agent authenticated successfully for API calls but its post-script push returned 403 because the emulator only allowed repository owners/site admins. | Implemented, tested with 22 Git transport/collaborator tests, rebuilt via `make host-rebuild-github`, and live collaborator verification passed. Code retry remains operator-triggered. |
| M12-020 | M12 | Split the repeatable development mint identities into separate App-backed users for triage, scribe, code, review, fix, and orchestration; Code and Fix now receive distinct write tokens. | Public Fullsend activity shows distinct agent bot identities at least for triage and retro. A single `fullsend-triage[bot]` actor was a local simplification and obscured per-agent provenance. | Complete; deployment bootstrap and M11 reseed succeeded. Live role verification resolved triage/scribe/code/review/fix/fullsend to their matching bot accounts. |
| M12-021 | M12 | Replaced blanket PR blocking and the synthetic `ready-for-merge` gate with persisted branch protection, a shared readiness evaluator, review/check/conflict policy enforcement, and event-driven auto-merge reevaluation. Added the GitHub CLI GraphQL fields required by `gh pr view` and `gh pr merge --auto`. | Fullsend expects GitHub to persist an auto-merge request and hold it until real branch requirements pass. The emulator previously could not distinguish policy blockers or execute the production-shaped CLI request. | Complete; 322 emulator tests passed, `make host-rebuild-github` rolled out successfully, and issue #34 automatically produced Triage 999 → Code 1003 → blocked PR #35 → Review 1005 → squash merge `15f8a771a4ab71a53c09434e4fa94adee2363c99`. |
| M12-022 | M12 | Made unconfigured `Repository.mergeQueue(branch:)` return GraphQL `null` and synchronized persisted base-branch SHAs after REST, web, and queued auto-merges. | An absent merge-queue field was interpreted by Fullsend as an active queue, causing it to omit the required merge-method flag; successful Git merges also left the branch API row stale. GitHub exposes a nullable field, and its branch API agrees with the Git ref. | Complete; Code run 1003 logged `Auto-merge: enabling on PR #35 (--squash)`, GraphQL showed the queued request owned by `fullsend-code[bot]`, and the post-merge branch API returned the resulting merge SHA. |
| M12-023 | M12 | Added an admin App reconciliation API and made the repeatable Fullsend bootstrap update existing App metadata and synchronize declared permissions into existing installations. Improved installation deletion and duplicate-create feedback in the admin UI. | The bootstrap treated an existing App as complete after HTTP 409, leaving the old Triage definition at only `issues: write`; newly installed repositories inherited incomplete permissions. Installation deletion and duplicate creation also redirected without identifying what happened. | Complete; 7 App lifecycle tests passed, the emulator and mint service were redeployed, and Fullsend Triage App installations for both `fullsend-dev/triage-target` and `admin/ansible-agent-harness` now have `contents: read`, `issues: write`, and `metadata: read`. |
| M12-024 | M12 | Added an admin-managed site-wide GitHub Actions runner scope, deployed one minimal shared router worker, and separated the `fullsend-router` label from centralized `fullsend` agent jobs. | Onboarded repositories should not require one runner Deployment each merely to invoke the reusable central dispatcher; matching lightweight jobs need an instance-wide worker while agent credentials and tooling remain centralized in `.fullsend`. | Complete; 15 emulator tests and 5 seed contract tests passed. `admin/ansible-agent-harness` run `1020`/job `1668` ran on `breadboard-site-router` and dispatched central Triage run `1021`, which completed successfully. |
| M12-025 | M12 | Replaced the lightweight site router with upstream GitHub Actions runner 2.317.0 registered at enterprise scope, added GitHub-compatible enterprise runner APIs and cross-repository broker scoping, and completed the real-runner job payload with events, step IDs/env/conditions/expressions, and signed job-scoped JWT authorization. | This makes the shared Fullsend router exercise GitHub's actual runner/configuration protocol while preserving repository isolation for repository-scoped real runners. The upstream worker requires a JWT-shaped OAuth credential before it launches a job. | Complete; 12 emulator tests and 5 seed contract tests passed. Target run `1034`/job `1682` succeeded on `breadboard-enterprise-router` and dispatched central `.fullsend` run `1035`. |
| M12-026 | M12 | Updated the central event router so any human comment on an issue currently labeled `needs-info` retriggers Triage; explicit `/fs-*` commands retain precedence and bot comments remain suppressed. | Upstream Fullsend expects a reporter's clarification to re-evaluate a `needs-info` issue automatically. The prior local router emitted a no-op target run unless the comment began with `/fs-triage`. | Complete; 6 seed contract tests passed. Ordinary comment #119 on `admin/ansible-agent-harness#5` produced target run `1041` and central Triage run `1042`. |
| M12-027 | M12 | Granted the Code, Review, and Fix bot identities collaborator access to `admin/ansible-agent-harness` after their selected-repository App installations were created. | The development mint returns bot-owned emulator PATs instead of real installation tokens, so App installation metadata alone does not authorize Git Smart HTTP pushes or role mutations. Code run `1050` completed its agent but failed its post-script push with HTTP 403. | Live authorization fixed; retry run `1052`/job `1700` completed successfully through the post-code script. It created no branch or PR because the agent could not read the externally hosted Gist and returned no implementation. General repository-onboarding automation remains a follow-up. |
| M12-028 | M12 | Added repeatable Fullsend repository onboarding and made GraphQL `createPullRequest` emit the same `pull_request_target: opened` event as REST creation. Onboarding installs the shim, reconciles role App installations and local bot access, and requires one approving review on `main`. | `gh pr create` uses GraphQL, so PR #6 was created without starting Review; the repository also lacked protection, causing Code to skip auto-merge. | Complete; 7 seed contract tests and the GraphQL event regression passed, `admin/ansible-agent-harness` was reconciled after `make host-rebuild-github`, and its workflow, role access, and branch protection were verified live. |
| M12-029 | M12 | Prevented a busy upstream Actions runner from claiming a second distributed-task job. | The runner's concurrent `status=Busy` control poll received job 1704 while job 1703 was still active; it acknowledged the message but never executed it, leaving the review router run permanently in progress. | Complete; 30 focused Actions tests passed, `make host-rebuild-github` redeployed the fix, orphaned run `1056` was cancelled, and no-op router run `1057` completed successfully on the real enterprise runner without invoking an agent stage. |
| M12-030 | M12 | Added GitHub-compatible closing-keyword resolution for PR descriptions and commit messages across REST, GraphQL, web, and auto-merge paths; implemented GraphQL `closingIssuesReferences` and `issues: closed` dispatch. | PR #6 contained `Closes #5`, but merging only closed the PR's backing issue row, leaving the referenced work item open. | Complete; default-branch enforcement, all nine documented keywords, optional colons, and same/cross-repository references are covered by 61 focused/adjacent tests. `make host-rebuild-github` deployed the change; live GraphQL linked PR #6 to issue #5, and historical issue #5 was repaired to `closed/completed`. |
| M12-031 | M12 | Added an owner selector to the emulator's `/ui/new` repository form and enforced active organization membership in both web and REST organization-repository creation. | Repository onboarding could only create under the signed-in user's namespace from the UI, even when that user belonged to organizations; the REST organization path also accepted unrelated authenticated users. | Complete; personal-owner backward compatibility and selection preservation on errors are covered by 23 focused tests. `make host-rebuild-github` deployed the change, and the live admin form lists `admin`, `experiment`, and `fullsend-dev` as owner choices. |
| M12-032 | M12 | Added an authorized, repository-scoped General settings page modeled on GitHub's layout, with persisted rename, default branch, description, website, visibility, template, and feature controls plus links to branch and runner administration. | The emulator had no repository Settings tab or web management surface, forcing repository configuration through APIs and preventing visual parity with GitHub onboarding workflows. | Complete; implemented with bare-repository rename/HEAD synchronization and responsive styling. Seventy-three broader web/API tests passed, `make host-rebuild-github` deployed the change, and Playwright validated the live owner/admin page and anonymous 403 behavior. |
| M12-033 | M12 | Changed Settings → Branches from the read-only branch browser to an authorized branch-protection rule list/editor backed by the emulator's existing protection models and merge-readiness evaluator. | Repository administrators need to configure required reviews, status checks, bypass behavior, linear history, force pushes, and deletion policy from the repository UI. | Complete; 75 broader web/REST/merge-readiness tests passed, `make host-rebuild-github` deployed the change, and Playwright validated the live rule list/editor against the protected `main` branch and two unprotected agent branches. |
| M12-034 | M12 | Made repository settings navigation persistent by sharing one sidebar across General, Branches, and a new settings-scoped Actions runners page. | Settings → Actions runners previously navigated to the standalone Actions view and removed the settings sidebar, breaking the expected GitHub settings navigation model. | Complete; 77 broader tests passed, `make host-rebuild-github` deployed the change, and Playwright clicked through General → Actions runners → Branches → General while confirming the settings sidebar remained visible on every page. |
| M12-035 | M12 | Added a GitHub-like Settings → Collaborators page with visibility summary, direct-access count, add/update/remove controls, and five repository permission levels; also required repository-admin authorization for REST collaborator mutations. | Repository onboarding and bot access previously required API/bootstrap operations, and the collaborator REST mutation endpoints accepted any authenticated user. | Complete; 91 broader tests passed, `make host-rebuild-github` deployed the change, and Playwright navigated through the persistent sidebar and validated the live repository's visibility, direct-access count, add form, and three existing collaborator rows. |
| M12-036 | M12 | Added Settings → Integrations → GitHub Apps with repository-filtered installation identity, account, installation ID, permission details, empty state, and admin configuration links. | App-backed access such as Fullsend Triage is intentionally absent from direct collaborators, but the repository UI had no place to expose installed Apps or explain their granted access. | Complete; 103 broader tests passed, `make host-rebuild-github` deployed the change, and Playwright navigated through the persistent sidebar and verified Fullsend Triage installation #2 with `contents: read`, `issues: write`, and `metadata: read` on the live repository. |
| M12-037 | M12 | Reorganized GitHub emulator production code under `src/`: the API/UI application is now `src/app`, the deterministic runner is `src/runners/emulator`, and the upstream Actions runner wrapper is `src/runners/upstream`. Updated packaging, containers, Compose, Vagrant, tests, deployment scripts, and documentation to use the new paths. | A single source root makes production code boundaries explicit while keeping tests, operational scripts, and database migrations separate. | Complete; all 357 emulator tests passed, Compose and application imports resolve from `src`, the emulator and all related runner images rebuilt, and live application imports, runner registration, heartbeats, and polling were verified. |
